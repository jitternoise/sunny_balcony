[gd_resource type="Resource" script_class="LevelData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/resources/LevelData.gd" id="1"]

[resource]
script = ExtResource("1")
level_id = 903
display_name = "Tutorial 3 — Steer the Stream"
intro_text = "Left alone, this stream runs off the edge of the board and the flood is wasted. You cannot move the water, but you can put something in its way. A Diverter (R) pushes water down and to the RIGHT. Tap it in the tray, then tap a hex to place it -- before you press play."
grid_radius = 3
water_sources = Array[Vector2i]([Vector2i(0, -3)])
fire_cells = Array[Vector2i]([])
pool_targets = {
Vector2i(-1, 1): 4
}
starting_inventory = {"divert_right": 1}
blocked_cells = Array[Vector2i]([])
town_cells = Array[Vector2i]([])
geyser_cells = Array[Vector2i]([])
total_block_budget = 0
