[gd_resource type="Resource" script_class="LevelData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/resources/LevelData.gd" id="1"]

[resource]
script = ExtResource("1")
level_id = 904
display_name = "Tutorial 4 — Think Again"
intro_text = "Two Diverters this time: one pushes left, one pushes right. Only one of them, in one place, saves this flood. Guessing is fine -- a block you have placed can be taken back by tapping it again, and you get it back in the tray. Try, look, change your mind."
grid_radius = 3
water_sources = Array[Vector2i]([Vector2i(0, -3)])
fire_cells = Array[Vector2i]([])
pool_targets = {
Vector2i(-3, 0): 4
}
starting_inventory = {"divert_left": 1, "divert_right": 1}
blocked_cells = Array[Vector2i]([])
town_cells = Array[Vector2i]([])
geyser_cells = Array[Vector2i]([])
total_block_budget = 0
